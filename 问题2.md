# 面试题全集（逐题简答）— 写入问题2.md

说明：以下为此前整理过的**全部问题**，每条均给出可直接口述的简短答案；不含示例代码。题量较多，可按章节检索。

---

## 第一部分：岗位、Java、中间件概览、架构、库表、前端联调、信创、AI、GIS、项目与行为

### Q1 描述一次你在客户现场或业主方参与需求分析的过程；对方诉求不清晰时你怎么推进？

**答：** 先说明背景（项目阶段、参与角色），再按「澄清现状—界定范围—确认优先级—形成纪要」来答。诉求不清晰时，用结构化提问把「谁在用、什么场景、频度、必须/可选、验收标准、与现有系统关系」问清；能现场演示或看原型最好；会后输出需求纪要与待确认清单，推动业主签字或邮件确认，避免口头需求无限扩散。

### Q2 技术方案和业务方有分歧时，你如何说服或折中？能否举一个公文或流程类系统的例子？

**答：** 先对齐目标（成本、工期、风险、合规），再用业务语言解释技术取舍（例如流程节点过多导致维护成本、与统一流程规范冲突）。公文类可举：留痕与性能、灵活流与审计要求、国产化控件兼容等。折中方式包括分期上线、先做默认规则后做特例、用配置代替硬编码。保留书面决策记录。

### Q3 联调、测试、上线阶段，你如何划分责任？遇到过扯皮吗？

**答：** 按接口契约划分：后端保证接口语义、错误码、鉴权与数据；前端保证调用顺序与展示；测试定义用例与边界；运维负责环境与发布窗口。问题单要有复现步骤与责任标签。扯皮时回到合同/需求/接口文档，用日志与抓包定位归属，避免在群里争论而无证据。

### Q4 线上问题紧急时，你的排查顺序是什么？如何同时安抚业务方？

**答：** 先止血（降级、限流、回滚、切只读），再定位（监控、日志、链路、最近变更），再修复与复盘。同步业务方：说明影响面、预计恢复时间、临时绕行方案，固定对接人，避免多人重复追问。恢复后给简短书面说明。

### Q5 外包驻场常见需求变更频繁，你如何控制范围、记录变更、保证可维护性？

**答：** 基线需求与变更分离，变更走评审与排期；用需求单或工单记录变更内容与确认人；技术上加开关、配置化、模块化，避免为单点需求污染核心代码；对「口头变更」要求补文档或邮件，否则不纳入迭代。

### Q6 你如何写和维护接口文档？与前端约定版本、兼容策略怎么做？

**答：** 统一用 Swagger/OpenAPI、YApi、Apifox 等之一，字段、错误码、示例请求响应写全；变更记版本号或变更日志；破坏性变更提前通知并保留旧字段过渡期；联调前冻结接口或打 tag。文档与代码同源维护可减少漂移。

### Q7 「代码规范落地」在你过往团队里是怎么做的？

**答：** 可答 Checkstyle/SpotBugs、格式化模板、分支策略、Mandatory Code Review、Sonar 门禁、提交信息规范、架构评审对公共模块强制规范。关键是团队共识与 CI 阻断，而不是只发文档。

### Q8 JVM 内存模型、常见 GC；什么场景会考虑调堆、调 GC？

**答：** 简述堆、栈、元空间、直接内存；Young/Old 分代与常见收集器（G1、CMS 等按你熟悉的说）。调堆：OOM、GC 频繁、停顿过长。调 GC：停顿敏感或吞吐不足时换收集器或调暂停目标、新生代比例等。强调先监控再调参，避免盲目加大堆。

### Q9 HashMap 与 ConcurrentHashMap 差异；高并发下 Map 使用注意点？

**答：** HashMap 非线程安全；ConcurrentHashMap 支持并发读写（实现因版本而异：分段锁或 CAS+ synchronized 桶）。高并发注意：不要用普通 HashMap 共享、注意复合操作需额外同步或使用 concurrent 包、注意 key 的 hash 与内存占用。

### Q10 线程池参数怎么设？拒绝策略、队列选型；政务类系统里批量任务你怎么设计？

**答：** corePool、maxPool、queue、keepAlive、拒绝策略（Abort/CallerRuns/Discard 等）要结合任务类型：IO 多可稍大线程，CPU 密集接近核数。队列用有界队列防内存打满。批量任务可拆分批次、异步队列、定时调度、限流，并对可重入任务保证幂等与进度记录。

### Q11 Spring Boot 自动装配原理？

**答：** 通过 `@EnableAutoConfiguration` 导入自动配置类，结合 `spring.factories`（或新版 imports）里声明的配置类，按条件注解（如 `@ConditionalOnClass`）决定是否生效，从而把默认 Bean 装配进容器。本质是约定优于配置 + 条件装配。

### Q12 Spring 事务传播行为、失效场景？

**答：** 传播行为包括 REQUIRED、REQUIRES_NEW、NESTED 等，各自适用嵌套、新事务场景。失效常见原因：自调用未走代理、异常类型被吞或不在 rollbackFor 范围、数据源非同一、非 public、引擎不支持事务的存储引擎误用等。

### Q13 Spring MVC 请求处理链路；过滤器、拦截器、AOP 顺序？

**答：** DispatcherServlet 映射 Handler，经拦截器链，调用 Controller，视图解析与返回。过滤器在 Servlet 容器层，先于 DispatcherServlet；拦截器在 Spring MVC 内；AOP 代理在 Bean 调用周围。具体顺序可答：Filter → Interceptor pre → Controller → Interceptor post → Filter 返回。

### Q14 Spring Cloud 与 Dubbo 选型你怎么说（若被问为什么用 OpenFeign 而不用 Dubbo）？

**答：** 诚实说明项目选型历史：团队栈、网关与 HTTP 生态、与前端和第三方对接便利、运维习惯等。Dubbo 擅长高性能二进制 RPC；Spring Cloud 擅长 REST 与整套微服务组件。没有绝对优劣，只有场景与成本。

### Q15 Hibernate 与 MyBatis；你们为何用 Hibernate？N+1、懒加载问题？

**答：** Hibernate 是 ORM，对象关联与懒加载方便但 SQL 可控性弱；MyBatis SQL 显式、易优化。选 Hibernate 常因遗留系统、快速 CRUD、领域模型。N+1 指关联多次查询，可用 join fetch、批量抓取、DTO 投影解决。懒加载注意事务边界与 Open Session In View 的利弊。

### Q16 Redis 数据类型与典型场景；缓存穿透、击穿、雪崩？

**答：** String/Hash/List/Set/ZSet 各举缓存、计数、排行榜等。穿透：查不存在绕过缓存；击穿：热点 key 过期瞬间压 DB；雪崩：大量 key 同时过期。对策：布隆过滤器或空值缓存、互斥重建或逻辑过期、随机 TTL 与集群限流。

### Q17 分布式锁如何实现？SET NX EX；与 Redisson 续期？

**答：** 用 SET key value NX EX 原子加锁，value 用唯一 token，释放用 Lua 比对 token 再删，避免误删他人锁。Redisson 用看门狗续期防止业务未执行完锁过期。要说明单 Redis 主从切换时的边界与 RedLock 争议，表示你了解风险。

### Q18 Redis 与数据库一致性怎么保证？

**答：** 强一致跨缓存与 DB 很难，一般用最终一致。常见 Cache Aside：读先缓存后库，写先更库再删缓存或短 TTL。跨系统用消息、版本号、对账。接受短暂不一致时用业务容忍度说明。

### Q19 Kafka 生产者、消费者基本模型；分区、副本、ISR？

**答：** 生产者向 Topic 写，Topic 分多个分区并行；每个分区多副本，Leader 对外服务，Follower 同步；ISR 是与 Leader 同步正常的副本集合，用于选举与 acks=all 语义。

### Q20 Kafka 如何保证消息不丢、不重复？

**答：** 不丢：生产者 acks=all、重试、broker 副本与 min.insync.replicas；消费者处理完再提交 offset，或事务消费。不重复：难以端到端绝对避免，靠幂等生产者减轻 broker 侧重复，业务侧幂等键、去重表、状态校验处理重复消费。

### Q21 Kafka 消费堆积如何排查？

**答：** 看消费 lag、分区分配是否均衡；查消费逻辑是否慢（DB、外部接口）、线程数、批量大小；Broker 磁盘与网络；是否频繁 Rebalance。针对性扩容分区与消费者、优化处理逻辑、限流上游。

### Q22 Kafka 在你们公文或文件场景里具体传什么？

**答：** 按你真实项目答：如异步通知、状态同步、审计日志、交换任务、大文件处理完成事件等。若无则答「可用于解耦峰值、异步通知、与第三方对接」，避免编造细节。

### Q23 Dubbo 若没用过，如何答？与 Spring Cloud 对比？

**答：** 诚实说项目用 Spring Cloud 服务调用，系统学习过 Dubbo 的注册发现、协议与容错概念。对比：Dubbo 默认二进制 RPC、性能与包体通常更优；Feign+HTTP 易调试、防火墙友好。表达愿意快速上手。

### Q24 Zookeeper 典型用途；CAP 下定位；与 Nacos 差异？

**答：** ZK 用于选主、配置、注册中心、分布式锁等，强一致 CP 倾向，会话与临时节点驱动上下线。Nacos 更偏服务发现与配置一体化，对云原生友好。可提 Kafka 新旧版本对 ZK 依赖变化（KRaft）作为知识面。

### Q25 微服务按业务域拆分的边界怎么划？拆错或过度拆分怎么补救？

**答：** 按限界上下文、数据归属、变更频率、团队边界划分。拆错则合并服务或抽取共享内核；过度拆分则合并部署、同步接口改异步、引入 BFF。强调演进式架构，不是一次到位。

### Q26 服务间同步调用与异步；什么场景用消息解耦？

**答：** 强一致短链路用同步；削峰、最终一致、多订阅者、上下游解耦、长耗时用消息。说明异步带来的复杂度：幂等、监控、死信队列。

### Q27 统一认证、CAS、多系统 SSO；ticket、session、跨域、登出联动？

**答：** CAS 通过 ST、TGT 等票据完成浏览器重定向登录；各子系统接 CAS Client 校验票据。跨域用回调地址白名单、SameSite、HTTPS。登出需单点登出协议或各系统 session 同步清理，否则出现部分系统仍登录。

### Q28 限流、熔断、降级在你们项目里有没有？没有怎么说？

**答：** 有则举 Sentinel/Hystrix/网关限流实例；没有则说在网关或 Nginx 层可做粗粒度限流，服务内可做超时与舱壁隔离，并说明高并发下的风险与改进计划。

### Q29 分布式事务：Seata、本地消息表、MQ 事务消息，你用过哪种？

**答：** 按实际答。未用过则解释各方案适用：Seata AT 适合 Java 微服务内强一致；本地消息表适合最终一致、实现简单；MQ 事务适合发消息与本地事务绑定。强调多数互联网场景用最终一致 + 幂等。

### Q30 索引设计原则；慢 SQL 分析步骤？

**答：** 高选择性列、最左前缀、避免过多索引写放大、覆盖索引适度使用。慢 SQL：看执行计划、是否全表扫描、索引是否命中、统计信息、数据倾斜，结合业务改索引或改写法。

### Q31 Oracle、达梦与 MySQL 在锁、分页、语法上的差异？

**答：** 分页语法不同；Oracle 行级锁与 MVCC 机制成熟；达梦兼容 Oracle 风格较多，迁移时注意函数、分页、序列、数据类型与工具链。强调迁移时充分测试与双轨校验。

### Q32 分库分表是否做过？什么量级会考虑？

**答：** 按实际。未做过则答：单表过大（行数与索引体积）、写入成为瓶颈、冷热数据分离时会考虑；Sharding 带来分布式事务与查询复杂度，需谨慎。

### Q33 历史数据迁移与新旧切换如何控制风险？

**答：** 双写或增量同步、全量校验、灰度切流、可回滚方案、冻结窗口、对账脚本。业务上提前培训与应急回退预案。

### Q34 与前端联调：鉴权 token、跨域、文件上传、分页参数常见坑？

**答：** Token 过期与刷新、时区、大小写、Header 名；跨域需后端允许或网关统一；大文件分片与超时、反向代理 body 大小限制；分页 page/size 与空结果、总数字段约定一致。

### Q35 JD 问 Vue2：生命周期、双向绑定、组件通信；你更熟 Angular 怎么说？

**答：** 简述 Vue2 生命周期与 v-model 是语法糖；props 下发、事件上报、Vuex。然后说明项目用 Angular，但 HTTP 与组件化思想相通，可快速补 Vue2 语法与项目规范。

### Q36 从后端角度谈前端页面性能优化？

**答：** 接口合并与分页、字段裁剪、压缩与缓存头、CDN 静态资源、减少轮询改推送或长轮询、异步加载大列表、服务端渲染视情况。强调与前端协作定义 SLA。

### Q37 大文件上传、断点续传前后端协议要点？

**答：** 分片大小、片序号、合并校验（MD5）、秒传（文件指纹）、失败重试与断点记录、合并接口幂等、存储与临时目录清理。

### Q38 信创环境软硬件栈你们实际组合？

**答：** 按简历答：麒麟等国产 OS、ARM/x86 路线、达梦、国产中间件、WPS 等。说明参与过适配与联调，突出问题解决经验。

### Q39 WPS、数科、软航等集成难点？

**答：** 控件兼容性、留痕套红格式、浏览器内核限制、Linux 与 Windows 差异、升级与许可证、与流程状态联动。强调与厂商协作与版本锁定。

### Q40 国产化中间件、达梦与 Oracle/MySQL 并存或迁移经验？

**答：** SQL 与函数兼容、工具导入导出、字符集、分页、存储过程迁移、性能对比测试、应用层 Hibernate 方言与驱动配置。

### Q41 信创机器部署与 x86 差异？

**答：** 依赖库路径、字体与渲染、指令集与 JDK 版本、安全加固策略、监控探针兼容性。强调验证清单与回归测试。

### Q42 日常怎么用 AI 工具？哪些任务收益最大？

**答：** 代码补全、样板代码、单测草稿、日志分析思路、文档整理、SQL 与正则。强调人工审查与安全边界。

### Q43 AI 生成代码的安全与版权；Code Review 重点？

**答：** 敏感信息不进公网模型；依赖许可证检查；逻辑与安全（注入、权限）必须人审；不盲信测试用例覆盖。

### Q44 团队推广 AI 工具的阻力与经验？

**答：** 信任度、规范不统一、答案质量波动；通过模板、案例、小范围试点、沉淀最佳提问方式与评审制度推进。

### Q45 LangChain、Agent：ReAct、工具调用、记忆是否了解？

**答：** Agent 通过规划-工具调用-观察循环完成任务；记忆分短期上下文与长期向量库；适用检索、工单分类等，要注意成本、幻觉与权限隔离。

### Q46 若把 Agent 引入公文检索或知识库，边界如何设计？

**答：** 权限与密级过滤、只读检索与生成摘要、引用出处、不可直接改业务数据除非走审批；审计日志与人工复核。

### Q47 是否考虑 Agent 用于研发流程？

**答：** 生成接口说明、生成单测骨架、Release note、运维脚本初稿；仍要 CI 与人审把关。

### Q48 是否接触过地图坐标系、WMS、WMTS、矢量切片？

**答：** 按实际。未做过则答了解 GIS 分层与切片概念，愿意结合政务一张图场景学习。

### Q49 Cesium、超图若有自学或 Demo 怎么说？没有怎么说？

**答：** 有则简述加载三维场景、图层与交互；无则诚实无项目经验，说明学习路径与与后端接口配合方式。

### Q50 微服务拆分前后部署、故障隔离、协作具体变化？

**答：** 独立发布与扩缩容、故障隔离更好；代价是分布式链路、运维复杂度上升。协作上接口契约与版本管理更重要。可补定性数据若无量化。

### Q51 流程引擎自由流与固定流的数据模型与状态机？

**答：** 固定流用预定义节点与转移条件；自由流允许动态加签、跳转，需更强版本控制与审计。状态机表达节点待办、进行中、驳回、结束等，注意并发与回退补偿。

### Q52 文件服务预览与编辑分离、格式转换异步化、失败重试？

**答：** 大转换放队列异步，前端轮询或 WebSocket 通知结果；重试要有上限与死信；预览走只读链路降低锁冲突。

### Q53 Kafka 在公文交换或异步任务里的端到端流程？

**答：** 按项目：谁生产、Topic、谁消费、失败补偿、顺序与幂等要求。无则描述典型事件驱动链路。

### Q54 CAS 单点登录与业务 session 一致性、单点登出？

**答：** 各系统可能仍有本地 session，需统一登出回调或缩短 session 与集中校验；注意跨域与 iframe 场景。

### Q55 最复杂的一次生产故障 STAR？

**答：** 准备真实案例：现象、影响、定位工具、根因、修复、预防措施（监控、开关、评审）。

### Q56 与住建局等单位的定制化与产品化如何平衡？

**答：** 核心产品化、差异配置化、个别插件化；需求分级与合并同类项；控制分支爆炸。

### Q57 为什么看外包驻场机会？对驻场节奏与加班期望？

**答：** 个人真实动机（技术栈、项目类型、地点），表达能接受合理驻场与沟通节奏，避免负面评价前公司过多。

### Q58 六年经验对三年 JD，如何避免被认为 overqualified 或不能踏实写代码？

**答：** 强调仍热爱编码与交付，愿意按团队规范做事，大包大揽与越权指挥不是你的风格；六年带来的是效率和排障能力。

### Q59 最近在读的技术或源码？

**答：** 准备真实材料（Spring、Netty、JDK 集合、Kafka 文档等），能说出收获即可。

---

## 第二部分：Kafka 拓展（逐题）

### K1 Topic、Partition、Replica、Leader、Follower、ISR 是什么？为什么要分区？

**答：** Topic 是逻辑分类；Partition 是有序日志分片，可并行；Replica 是副本；Leader 负责读写该分区；Follower 同步；ISR 是同步正常的副本集合。分区用于提升吞吐、并行消费、按 key 保序。

### K2 生产者发送消息的完整路径？

**答：** 序列化、分区器选分区、进入缓冲区批量聚合、Sender 线程发送到 Broker、收到 ack 与元数据回调。

### K3 消费者组与分区分配：一个分区能否被多个消费者同时消费？

**答：** 同一消费者组内，一个分区同一时刻只能被一个消费者消费；不同组可重复读。消费者多于分区时会有空闲实例。

### K4 `auto.offset.reset`：latest、earliest、none？

**答：** earliest 从最早可读开始；latest 只读新数据；none 无位移则报错强制人工处理。新组是否消费历史取决于该配置。

### K5 位移（offset）存在哪里？旧版 ZK 与新版 `__consumer_offsets`？

**答：** 新版存在内部 Topic `__consumer_offsets`；老版本曾用 ZK，已逐步淘汰该路径，了解即可。

### K6 高水位 HW、LEO 是什么？与消费者可见消息关系？

**答：** LEO 是副本下一条写入位置；HW 是 ISR 已同步到的安全水位，消费者默认只能读到 HW 之前，避免 Leader 切换丢已读未提交语义混乱。

### K7 生产者 acks=0/1/all 区别与吞吐、丢数据权衡？

**答：** 0 最快最易丢；1 等 Leader 落盘；all 等 ISR 满足配置才确认，最稳但延迟上升。生产写路径常用 all 加重试与幂等。

### K8 `min.insync.replicas` 与 acks=all 如何配合？

**答：** 防止只有 Leader 确认但副本不足的情况；副本数不够时写入失败优于默默丢数据。

### K9 幂等生产者原理？能解决与不能解决什么？

**答：** 通过生产者 ID 与序列号去重同一生产者会话内重复写入。不能替代业务跨系统幂等。

### K10 事务生产者、事务 ID、`read_committed` 隔离级别、典型场景？

**答：** 把多条消息以事务提交，消费者可选只读已提交，实现「写多分区」原子可见性。用于流处理与精确一次链路的一部分，仍有性能与使用复杂度。

### K11 「至少一次」「至多一次」「精确一次」在 Kafka 里怎么配置、代价？

**答：** 至多一次少重试；至少一次默认常见；精确一次需幂等加事务与配合消费者，成本高且跨 DB 仍要业务方案。

### K12 消费者手动提交与自动提交；重复消费与丢消息易出现在哪？

**答：** 自动提交可能「未处理完就提交」导致重启丢消息；手动提交若先提交后处理可能丢，先处理后提交可能重复。需与幂等结合。

### K13 分区数怎么定？过多过少问题？

**答：** 结合吞吐与消费者并行；过少并行不足；过多文件句柄、元数据、Rebalance 成本上升。

### K14 如何评估单分区吞吐上限？瓶颈在哪？

**答：** 压测看磁盘顺序写、网络、压缩 CPU；单分区有序，分区是扩展单元。

### K15 批量发送参数 linger.ms、batch.size 如何调？

**答：** linger 增大换吞吐、增延迟；batch 控制单批大小。在延迟与吞吐间权衡。

### K16 压缩类型 gzip、snappy、lz4、zstd 选型？

**答：** CPU 与带宽权衡：lz4/zstd 常用；gzip 压缩率高但 CPU 重。

### K17 日志保留策略 retention、compact topic 适用场景？

**答：** 时间或大小淘汰；compact 保留每个 key 最新值，适合 changelog、状态存储。

### K18 消费 lag 持续升高如何排查？

**答：** 消费慢、分区少、线程不足、下游依赖慢、GC、Rebalance；逐项排除。

### K19 Rebalance 频繁触发原因？

**答：** 会话超时、心跳问题、处理超过 max.poll.interval、扩缩容消费者、网络抖动。

### K20 消息一直重复可能原因？

**答：** 消费重试、提交失败、Rebalance 重复分配、生产重试；业务必须幂等。

### K21 Broker 磁盘打满、单分区过大现象与处理？

**答：** 写入失败、ISR 异常；扩容磁盘、调整保留、增加分区（增分区不改变已有 key 分布需知）、清理非法数据。

### K22 跨机房、跨集群镜像是否了解？

**答：** MirrorMaker 等工具做集群复制；注意延迟、重复与合规；多用于灾备或聚合分析。

### K23 Kafka 与 RocketMQ、RabbitMQ 模型差异与选型？

**答：** Kafka 高吞吐日志型；RocketMQ 事务与延迟消息等特性丰富；RabbitMQ 传统队列与路由灵活。按吞吐、顺序、生态、运维选型。

### K24 用 Kafka 做请求同步响应合适吗？

**答：** 不合适为主；同步请求用 RPC/HTTP；Kafka 适合异步事件。

### K25 事件溯源、CQRS 里 Kafka 常扮演什么角色？

**答：** 作为事件总线持久化业务事件，支持投影重建与审计。

### K26 顺序消息：同一业务键如何保证同一分区？

**答：** 同一 key 进同一分区器映射到固定分区，分区内有序。

### K27 延迟队列、定时消息在 Kafka 里如何近似实现？

**答：** 独立延迟 Topic 加时间轮、或按触发时间分区、或外部调度器到点发消息；精确延迟常用专门中间件。

### K28 Spring Kafka 中 KafkaTemplate、`@KafkaListener` 常用配置项？

**答：** 序列化、消费者组、并发度、ack 模式、错误处理器、重试与 DLQ 集成等，按项目实际列举。

### K29 批量消费、手动 ack、错误处理 Seek、DLQ 设计？

**答：** 批量拉取提高吞吐；失败进入死信 Topic 或重试队列；Seek 回退 offset 需谨慎防死循环。

### K30 Spring Cloud Stream 与原生客户端取舍？

**答：** Stream 抽象 binder 换中间件成本低；原生客户端灵活可控。团队熟悉度与抽象泄漏决定选型。

---

## 第三部分：Redis 拓展（逐题）

### R1 各数据类型典型场景（各举两类）？

**答：** String 缓存与计数；Hash 对象字段；List 队列与最新列表；Set 去重与集合运算；ZSet 排行榜与按分数范围查询。

### R2 SET key NX EX 与 SETNX 区别？

**答：** SET NX EX 一条命令原子设置值与过期，推荐；SETNX 往往需另设过期，竞态下易死锁或永不过期。

### R3 HyperLogLog、Bitmap、GEO 用途？

**答：** HyperLogLog 近似去重计数省内存；Bitmap 签到与位图统计；GEO 附近的人与距离计算（需了解命令限制）。

### R4 Pipeline 作用；与事务 MULTI/EXEC 区别？

**答：** Pipeline 批量减少 RTT，不保证原子；事务将队列一次执行但不支持回滚语义如关系数据库。

### R5 Lua 脚本解决什么问题？

**答：** 多条命令在服务端原子执行，减少往返，适合库存扣减与安全释放锁。

### R6 RDB 与 AOF 各有什么特点？

**答：** RDB 为定时或手动快照，文件紧凑、恢复快，但两次快照之间的数据可能丢失。AOF 追加写命令，数据更完整，文件变大且需 rewrite 维护。「RDB+AOF 混合」机制单独见 R25。

### R7 AOF rewrite 触发与对性能影响？

**答：** 重写压缩 AOF 体积；fork 与子进程写时复制可能带来短暂抖动，需监控。

### R8 内存淘汰策略如何选择？

**答：** 结合业务是否允许丢缓存：allkeys-lru 与 volatile-ttl 等；必须设置 maxmemory 防止 OOM。

### R9 大 key、热 key 如何发现与治理？

**答：** 扫描、延迟、内存分析；大 key 拆分或异步删除 UNLINK；热 key 本地缓存、随机后缀分散、读写分离。

### R10 主从复制原理；复制延迟导致问题？

**答：** 主写从异步拉取；延迟导致读到旧数据，需业务容忍或读主策略。

### R11 Sentinel 故障转移流程；脑裂？

**答：** 监控下线投票选新主；脑裂指分裂网络下多主写入，需 quorum 与 min-slaves 等配置降低风险。

### R12 Redis Cluster 槽位、MOVED/ASK；跨 slot 事务限制？

**答：** 16384 槽分片；键不在本节点返回 MOVED；迁移中 ASK；多 key 事务需同槽可用 hash tag。

### R13 缓存穿透、击穿、雪崩及方案？

**答：** 同 Q16，强调布隆、互斥、随机 TTL、集群保护。

### R14 Cache Aside、Read Through、Write Behind？

**答：** Aside 应用管缓存；Read Through 由缓存组件代查库；Write Behind 异步写库提高写性能但一致性与丢失风险更大。

### R15 先删缓存再写 DB 与先写 DB 再删缓存并发问题？

**答：** 两种顺序在并发下都可能出现短暂不一致；常用先更库再删缓存加延迟双删或版本号，并接受最终一致。

### R16 延迟双删是否必要？

**答：** 在高并发读旧值回写缓存场景可考虑；增加复杂度和延迟，不是银弹。

### R17 本地缓存加 Redis 二级缓存一致性问题？

**答：** 本地 TTL 短、订阅失效、版本号同步；否则易出现本地脏读。

### R18 单节点 Redis 分布式锁缺陷；Redisson 续期；RedLock 争议？

**答：** 主从切换可能丢锁；Redisson 看门狗续期；RedLock 在时钟漂移与 CP 讨论上有争议，面试说明知局限即可。

### R19 库存扣减、秒杀场景 Lua 与 DB 乐观锁如何配合？

**答：** Redis 扛并发预减与排队，数据库层唯一约束或版本号兜底，防止超卖。

### R20 阻塞命令 KEYS、大聚合为何生产禁用？

**答：** 阻塞单线程导致延迟尖刺，可能拖垮集群。

### R21 慢查询日志如何分析？

**答：** 开启 slowlog，看命令与参数规模，优化算法与数据结构。

### R22 INFO 里哪些指标要重点看？

**答：** used_memory、连接数、命中率、复制积压、持久化状态等。

### R23 Redis 6/7 多线程指什么？对单 key 热写帮助？

**答：** IO 线程辅助网络读写，命令执行主线程仍单线程，热 key 根本解决靠架构而非 IO 线程。

### R24 Spring Cache、Lettuce/Jedis 连接池配置注意点？

**答：** 超时、最大连接、空闲检测；避免连接泄漏与过长阻塞。

### R25 「混合持久化」单独说明：在什么版本、解决什么问题？

**答：** Redis 4.0 起支持在 AOF 重写时把当前数据集以 RDB 格式写入 AOF 头部，其后追加增量 AOF，兼顾重启速度与数据安全性，是 RDB 与 AOF 的折中方案。

### R26 Jedis 与 Lettuce 在线程模型与使用上的差异？

**答：** Jedis 多为阻塞式、一连接一线程需注意；Lettuce 基于 Netty 异步非阻塞，与 Spring Data Redis 2.x 默认搭配更好，支持响应式 API，高并发下资源利用更友好。

---

## 第四部分：Dubbo 拓展（逐题）

### D1 Dubbo 整体分层？

**答：** 配置、代理、注册、集群路由、协议、传输等分层，职责清晰可扩展。

### D2 Provider、Consumer、Registry、Monitor、Container 职责？

**答：** 服务方、调用方、注册与发现、监控统计、运行容器（可选概念）。

### D3 一次 RPC 从 Consumer 到 Provider 链路简述？

**答：** 代理调用 → 路由负载均衡 → 序列化 → 网络 → 反序列化 → 反射执行实现 → 返回。

### D4 注册中心 ZK、Nacos 与临时节点、会话超时关系？

**答：** 临时注册随会话失效删除，体现上下线；超时设置影响抖动与误判。

### D5 服务接口如何注册？版本号、分组怎么用？

**答：** 注解或 XML 声明接口与实现；版本与分组用于灰度、多租户、接口共存。

### D6 多环境 dev/test/prod 服务隔离怎么做？

**答：** 命名空间、分组、元数据标签、不同注册集群；配置中心隔离。

### D7 元数据中心作用？

**答：** 存服务定义、参数列表等，支撑应用级服务发现与控制台治理。

### D8 负载均衡策略 random、roundrobin、leastactive、consistenthash 适用？

**答：** random 默认；roundrobin 均匀；leastactive 抗慢节点；一致性哈希亲和会话或缓存。

### D9 Cluster 容错 failover、failfast、failsafe、forking 含义与选型？

**答：** failover 失败切换；failfast 快速失败适合写；failsafe 忽略失败适合日志；forking 并行取快适合幂等读。

### D10 路由规则：条件路由、标签路由、黑白名单场景？

**答：** 按参数、来源、标签把流量导向指定集群，用于灰度与隔离。

### D11 泛化调用用在什么场景？

**答：** 网关、测试平台、无 API jar 的调用方，用接口名字符串调用。

### D12 dubbo 协议与 HTTP、gRPC 对比？

**答：** dubbo 二进制高效；HTTP 易调试；gRPC 跨语言与 HTTP/2 流式强。

### D13 Hessian2、Kryo、Protobuf 序列化注意？

**答：** 兼容性、体积、演进、安全（反序列化漏洞）与跨语言需求。

### D14 大报文、文件传输是否适合走 Dubbo？

**答：** 一般不适合，改用文件服务、对象存储、分块 HTTP 或专用通道。

### D15 异步调用、事件通知 oneway 区别与线程模型？

**答：** 异步返回 Future 或回调；oneway 不等结果，适合日志类，注意线程池与背压。

### D16 过滤器链：鉴权、限流、日志埋点怎么扩展？

**答：** SPI 注册 Filter，在调用前后织入，注意顺序与异常处理。

### D17 服务降级、mock 配置方式与熔断关系？

**答：** mock 用于测试或降级返回默认值；熔断在失败率超阈值时快速失败，二者可配合。

### D18 超时、重试次数设置不当会导致什么？

**答：** 雪崩、线程池耗尽、重复写、延迟堆积。写操作建议 retries 谨慎。

### D19 RpcContext 传递 traceId、用户信息？

**答：** 隐式传参，在 Filter 中设置与读取，注意线程清理避免泄漏。

### D20 Dubbo 加 Nacos 与 Spring Cloud OpenFeign 加 Nacos 差异与选型？

**答：** 见 Q14，补充 Nacos 可同时作注册与配置。

### D21 双协议、多注册中心是否了解？

**答：** 平滑迁移、灰度、内外网隔离场景；注意客户端路由复杂度。

### D22 Dubbo3 应用级服务发现与接口级区别？

**答：** 应用级减少元数据量、提升规模；需理解服务映射关系。

### D23 调用超时、线程池满如何排查？

**答：** 看线程栈、慢调用、下游 RT、队列长度；调整超时与隔离线程池。

### D24 序列化异常、类版本不一致常见报错与处理？

**答：** 统一 API 版本、兼容字段、避免私自改包名；灰度发布协调。

### D25 注册中心抖动时服务调用表现与防护？

**答：** 本地缓存地址、合理重试退避、熔断、监控告警。

---

## 第五部分：Vue2 拓展（逐题）

### V1 `el` 与 `$mount` 关系；运行时加编译版 vs 仅运行时？

**答：** el 指定挂载根节点，也可用 `$mount` 手动挂载。完整版含模板编译器；运行时版体积小需预编译模板。

### V2 `v-if` 与 `v-show`；`v-for` 中 key 作用？

**答：** v-if 真条件渲染；v-show 切 display。key 标识 vnode 身份，稳定 diff。

### V3 `v-model` 在表单与自定义组件；`.sync` 修饰符？

**答：** v-model 默认 prop+事件语法糖；`.sync` 用于父子双向绑定多个属性（Vue2.3），与 v-model 类似注意命名。

### V4 列表渲染为何不建议 index 作为 key？

**答：** 列表重排或插入删除时 index 变化导致复用错误与组件状态错乱。

### V5 `data` 为何必须是函数返回对象？

**答：** 避免组件多实例共享同一 data 引用。

### V6 `computed` 与 `methods` 与 `watch` 使用场景？

**答：** computed 有缓存适合派生状态；methods 每次调用；watch 适合异步或深度监听副作用。

### V7 `watch` 的 deep、immediate；`$watch` API？

**答：** deep 监听嵌套对象；immediate 立即执行一次；`$watch` 编程式监听便于动态注销。

### V8 生命周期钩子完整顺序；父子组件钩子顺序？

**答：** beforeCreate→created→beforeMount→mounted→更新钩子→销毁钩子。父先子后 mounted，子先父 destroyed（常考父子销毁顺序）。

### V9 `beforeDestroy`/`destroyed` 里常做什么？

**答：** 清定时器、解绑全局事件、销毁第三方实例，防止内存泄漏。

### V10 props 单向数据流；父改子、子通知父？

**答：** 子不应直接改 prop，用 data 拷贝或 emit 事件让父改。

### V11 `provide`/`inject` 使用场景与响应式陷阱？

**答：** 跨层级传依赖；provide 非响应式若传原始值子组件不更新，传对象引用或 observable。

### V12 兄弟组件通信方式？

**答：** 事件总线（慎用全局污染）、共同父中转、Vuex。

### V13 `$refs` 使用注意？

**答：** 避免过度操作 DOM；refs 在 mounted 后才可靠；不应用 ref 替代数据驱动。

### V14 自定义指令钩子；与 Vue3 差异？

**答：** bind/inserted/update 等；Vue3 生命周期命名变化，了解迁移方向即可。

### V15 动态组件 `<component :is>`、`keep-alive` 与 `activated`？

**答：** 动态切换组件；keep-alive 缓存实例；activated/deactivated 在缓存命中时调用。

### V16 默认插槽、具名插槽、作用域插槽各一例说明？

**答：** 默认内容占位；多区域布局用具名；子传数据给父插槽内容用作用域插槽。

### V17 `hash` 与 `history` 模式；history 服务端要配什么？

**答：** hash 用 # 路由不请求服务器路径；history 用正常 URL，需服务器将所有前端路由 fallback 到 index.html。

### V18 导航守卫：全局、路由独享、组件内；执行顺序？

**答：** 全局 beforeEach→路由独享→组件内 beforeRouteEnter 等，进入与离开成对理解；具体顺序可背官方文档顺序表。

### V19 路由懒加载与打包体积关系？

**答：** `import()` 拆 chunk，首屏变小；注意 prefetch 策略与过多小文件权衡。

### V20 `beforeEach` 里根据 token 跳转典型逻辑？

**答：** 无 token 且需鉴权则重定向登录并带 redirect；有 token 访问登录页可跳首页。

### V21 Vuex 中 State、Getter、Mutation、Action 分工；为何 mutation 必须同步？

**答：** mutation 用于可追踪的状态变更且要求同步便于 devtools 记录；异步放 action。

### V22 模块化 modules 命名空间；跨模块调用？

**答：** namespaced 避免命名冲突；通过 rootState 或 dispatch 根模块协调。

### V23 组件本地 state 与 Vuex 如何划分？

**答：** 仅组件关心的 UI 状态放本地；跨页面共享、缓存、权限等放 Vuex。

### V24 列表性能优化：虚拟滚动、分页、`Object.freeze`？

**答：** 大列表虚拟化或后端分页；只读大数据可 freeze 减少响应式开销。

### V25 组件销毁后仍更新 state 的警告原因与避免？

**答：** 异步回调在销毁后仍 setState；在回调里判断已销毁或清理定时器与请求。

### V26 `nextTick` 原理与使用场景？

**答：** DOM 更新异步批量；需要在数据变更后立即读新 DOM 或滚动到底部时使用。

### V27 Webpack/Vue CLI 代码分割、公共资源缓存策略？

**答：** vendor 分离、hash 文件名长期缓存、runtime 单独拆避免缓存失效问题。

### V28 与后端联调：axios 拦截器 token、错误码、401 跳转？

**答：** 请求头注入 token；统一错误处理；401 清 token 跳登录并防循环。

### V29 Vue2 响应式原理与局限；`$set` 作用？

**答：** defineProperty 劫持；无法检测新增属性与数组下标直接赋值；`$set` 补响应式。

### V30 虚拟 DOM diff 思路；与直接操作 DOM 取舍？

**答：** 用 js 描述树 diff 最小更新；适合复杂 UI 状态同步；简单场景直接 DOM 也可但不推荐破坏框架一致性。

### V31 Vue2 与 Vue3 差异一句话？

**答：** Vue3 用 Proxy、Composition API、性能更好；Vue2 选项式 API 与生态成熟。

### V32 跨域：开发环境 proxy；生产由网关或 Nginx？

**答：** 开发 devServer 代理；生产同源策略由网关反向代理或 CORS 配置解决。

### V33 上传文件、大文件分片在前端如何组织？

**答：** 切片、并发控制、进度合并、完成后通知后端合并与校验。

### V34 表格分页：前端分页与后端分页与接口字段约定？

**答：** 数据量大用后端的 page/size/total；前端分页仅适合小数据；字段命名与空态约定写进文档。

---

## 第六部分：交叉与其它

### X1 Dubbo 超时加重试与下游 Kafka 发送失败重试叠加，如何避免放大写？

**答：** 限制重试次数与退避；写路径幂等；隔离线程池；熔断慢调用；消息发送与业务事务解耦用 Outbox。

### X2 用 Redis 存消费进度与 Kafka 自身 offset 什么场景混用？

**答：** 特殊Exactly-once自制方案或外部调度，但主流仍用 Kafka offset；混用增加一致性与运维复杂度，一般不推荐除非有强理由。

### X3 前端 Vue 列表页加后端分页加 Redis 缓存列表的一致性与缓存 key 设计？

**答：** key 包含查询条件与分页参数；更新数据时删相关 key 或短 TTL；强一致则少缓存列表。

### X4 接口幂等：Redis token、业务唯一键、Kafka 幂等生产者能否讲清边界？

**答：** Redis token 防重复提交；唯一键防重复落库；Kafka 幂等防生产端重复写入日志；三者解决不同层次，业务仍需消费幂等。

---

## 附：题号与文件内对应关系（便于自检）

- 第一部分：Q1–Q59（岗位至行为）
- 第二部分：K1–K30（Kafka）
- 第三部分：R1–R26（Redis）
- 第四部分：D1–D25（Dubbo）
- 第五部分：V1–V34（Vue2）
- 第六部分：X1–X4（交叉）

合计 **178** 道独立简答题。若你还希望把「第一轮清单」里与 K/R/D/V 完全重复的题拆成更细的子问题，可在此文件后续自行追加编号。
